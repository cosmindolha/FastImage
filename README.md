# FastImage

Tiny SwiftUI image viewer for macOS.

FastImage opens standard images and camera RAW files, including Sony `.arw`, using macOS's built-in image codecs. RAW files use their embedded preview where available so opening and navigation stay fast without adding a heavyweight decoding dependency.

<img src="FastImage app.jpg" width="512"/>
